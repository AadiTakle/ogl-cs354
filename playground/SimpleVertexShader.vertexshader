#version 330 core

// Input vertex data, different for all executions of this shader.
layout(location = 0) in vec3 vertexPosition_modelspace;

uniform mat4 MVP;

void main(){
	// Apply MVP transformations to triangle
    gl_Position = MVP * vec4(vertexPosition_modelspace, 1.0);

}

